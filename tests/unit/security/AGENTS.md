# Security tests agent map

Read repository `AGENTS.md`, `ARCHITECTURE.md` and `docs/architecture/execution-boundaries.md` before editing.
These tests cover secret storage, redaction and runtime-safe configuration. Never add real credentials; use deterministic fakes and fixtures. Run focused security tests, then repository gates.
