# services/notifications test map

Read repository `AGENTS.md`, `ARCHITECTURE.md` and the nearest source `AGENTS.md` before editing.
Keep tests deterministic and offline; use fixtures/fakes for provider boundaries and never add credentials. This suite covers `services/notifications` and should be run before repository quality gates.
