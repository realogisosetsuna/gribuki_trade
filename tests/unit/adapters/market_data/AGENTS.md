# adapters/market_data test map

Read repository `AGENTS.md`, `ARCHITECTURE.md` and the nearest source `AGENTS.md` before editing.
Keep tests deterministic and offline; use fixtures/fakes for provider boundaries and never add credentials. This suite covers `adapters/market_data` and should be run before repository quality gates.
