# Strategy lab 测试地图

本目录覆盖研究实验、factor DSL、walk-forward、退出策略评估和序列化。
实现位于 `src/gribuki_trade/strategy_lab/`；输出必须保持
`research_only`，不得提升为 PAPER 或 LIVE 配置。

运行 `python -m pytest --temp-dir runtime/strategy-lab-tests -q tests/unit/strategy_lab`。
