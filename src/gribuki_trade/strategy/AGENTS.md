# Strategy agent map

Read repository `AGENTS.md`, `ARCHITECTURE.md` and `docs/architecture/module-map.md` before editing.
Strategies consume ports/services and point-in-time features only; keep execution authority in trading/services and preserve research-only boundaries. Tests live in `tests/unit/strategy/` and `tests/unit/strategy_lab/`. Run focused tests, then repository gates.
