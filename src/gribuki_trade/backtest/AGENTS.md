# Backtest agent map

Read repository `AGENTS.md`, `ARCHITECTURE.md` and `docs/architecture/module-map.md` before editing.
Backtests are research-only and must remain deterministic and broker-free; do not promote results to PAPER or LIVE. Tests live in `tests/unit/backtest/`. Run focused backtest tests, then repository gates.
