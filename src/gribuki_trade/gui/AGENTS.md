# GUI agent map

Read repository `AGENTS.md`, `ARCHITECTURE.md` and `docs/architecture/module-map.md` before editing.
GUI code depends on integration gateways and services, not provider clients; preserve PAPER/SHADOW/LIVE guards and non-blocking behavior. Tests live in `tests/unit/gui/`. Run focused GUI tests, then repository gates.
