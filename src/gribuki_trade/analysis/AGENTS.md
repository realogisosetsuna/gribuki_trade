# Analysis agent map

Read repository `AGENTS.md`, `ARCHITECTURE.md` and `docs/architecture/module-map.md` before editing.
Keep report schemas and analytical models pure: depend on domain values, not adapters, brokers or network clients. Tests live in `tests/unit/analysis/`. Run focused analysis tests, then repository gates.
