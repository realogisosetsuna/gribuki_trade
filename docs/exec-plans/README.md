# ExecPlans

Create one Markdown file in `active/` before a significant refactor. Record the
objective, scope, evidence, decisions, validation and completion criteria.
Keep the plan current while work is in progress. Move it to `done/` only after
the stated validation and review are complete. ExecPlans describe durable work
state; short-lived exploration belongs in the conversation.
